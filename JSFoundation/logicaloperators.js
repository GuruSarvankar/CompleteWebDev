// &&   - and
// ||   - or
// !    - reverse

isLoggedin = false;
isPaid = false;

console.log(isLoggedin && isPaid); // result will false because both var should be true or false

let googleUser = true;
let facebookUser = false;

console.log(googleUser || facebookUser); // result will be true bcuz atleast 1 should be true
